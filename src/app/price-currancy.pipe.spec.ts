import { PriceCurrancyPipe } from './price-currancy.pipe';

describe('PriceCurrancyPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceCurrancyPipe();
    expect(pipe).toBeTruthy();
  });
});
