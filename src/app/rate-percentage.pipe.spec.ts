import { RatePercentagePipe } from './rate-percentage.pipe';

describe('RatePercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new RatePercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
